export const setMyUser = payload => (
    {
      type : 'SET_MYUSER',
      payload,
    }
);

export const addBalance = payload => (
    {
      type : 'ADD_BALANCE',
      payload,
    }
);

export const addUser = payload => (
  {
    type : 'ADD_USER',
    payload,
  }
);

export const sendCoins = payload => (
    {
      type : 'SEND_COINS',
      payload,
    }
);
    